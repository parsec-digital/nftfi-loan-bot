{
  "type": "module",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "@nftartloans/shared": "link:shared",
    "mocha": "^10.1.0"
  },
  "scripts": {
    "test": "mocha test/* -r dotenv/config --trace-warnings --parallel --exit",
    "run-fn": "functions-framework --target=handle"
  }
}
